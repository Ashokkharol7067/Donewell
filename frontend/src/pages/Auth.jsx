import { useState } from 'react'
import { Link, Navigate, useLocation, useNavigate } from 'react-router-dom'
import { ArrowRight, Check, LockKeyhole } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export default function Auth({ mode }) { const { user, login, register } = useAuth(); const navigate = useNavigate(); const location = useLocation(); const isLogin = mode === 'login'; const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const [busy, setBusy] = useState(false); if (user) return <Navigate to="/" replace />; async function submit(e) { e.preventDefault(); setBusy(true); setError(''); try { await (isLogin ? login({ email: form.email, password: form.password }) : register(form)); navigate(location.state?.from || '/') } catch (err) { setError(err.response?.data?.message || 'Unable to connect to the workspace') } finally { setBusy(false) } } return <div className="auth-page"><div className="auth-art"><div className="brand light"><span className="brand-mark">✓</span> Donewell</div><div className="art-copy"><p className="kicker">A calmer way to work</p><h1>Make room for<br/><em>good work.</em></h1><p>Keep the important things visible, moving, and under control.</p></div><div className="quote"><Check size={16}/> Focused teams finish more together.</div></div><div className="auth-panel"><div className="auth-top"><LockKeyhole size={18}/><span>Private workspace</span></div><h2>{isLogin ? 'Welcome back' : 'Create your workspace'}</h2><p className="muted">{isLogin ? 'Sign in to pick up where you left off.' : 'Start organizing your work in minutes.'}</p><form onSubmit={submit}>{!isLogin && <label>Full name<input required value={form.name} onChange={(e) => setForm({ ...form, name: e.target.value })} placeholder="Alex Morgan"/></label>}<label>Email address<input required type="email" value={form.email} onChange={(e) => setForm({ ...form, email: e.target.value })} placeholder="you@company.com"/></label><label>Password<input required minLength="6" type="password" value={form.password} onChange={(e) => setForm({ ...form, password: e.target.value })} placeholder="At least 6 characters"/></label>{error && <div className="error">{error}</div>}<button className="primary full" disabled={busy}>{busy ? 'Please wait...' : isLogin ? 'Sign in' : 'Create account'} <ArrowRight size={17}/></button></form><p className="switch">{isLogin ? 'New to Donewell?' : 'Already have an account?'} <Link to={isLogin ? '/register' : '/login'}>{isLogin ? 'Create an account' : 'Sign in'}</Link></p></div></div> }
