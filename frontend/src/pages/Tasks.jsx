import { useEffect, useState } from 'react'
import { Check, Edit3, Plus, Search, Trash2 } from 'lucide-react'
import { Link } from 'react-router-dom'
import api from '../api'
export default function Tasks() { const [tasks, setTasks] = useState([]); const [filters, setFilters] = useState({ search: '', status: '', priority: '' }); const [loading, setLoading] = useState(true); async function load() { setLoading(true); const { data } = await api.get('/tasks', { params: filters }); setTasks(data.tasks); setLoading(false) } useEffect(() => { const timer = setTimeout(load, 250); return () => clearTimeout(timer) }, [filters.status, filters.priority, filters.search]); async function toggle(task) { const { data } = await api.put(`/tasks/${task._id}`, { ...task, status: task.status === 'pending' ? 'completed' : 'pending' }); setTasks(tasks.map((item) => item._id === task._id ? data.task : item)) } async function remove(id) { if (window.confirm('Delete this task?')) { await api.delete(`/tasks/${id}`); setTasks(tasks.filter((task) => task._id !== id)) } } return <section><div className="section-head"><div><p className="kicker">YOUR WORK</p><h2>All tasks <span className="count">{tasks.length}</span></h2></div><Link className="primary" to="/tasks/new"><Plus size={17}/> New task</Link></div><div className="filters"><div className="search"><Search size={17}/><input placeholder="Search tasks..." value={filters.search} onChange={(e) => setFilters({ ...filters, search: e.target.value })}/></div><select value={filters.status} onChange={(e) => setFilters({ ...filters, status: e.target.value })}><option value="">All statuses</option><option value="pending">Pending</option><option value="completed">Completed</option></select><select value={filters.priority} onChange={(e) => setFilters({ ...filters, priority: e.target.value })}><option value="">All priorities</option><option value="high">High priority</option><option value="medium">Medium priority</option><option value="low">Low priority</option></select></div><div className="task-list">{loading ? <div className="empty">Loading tasks...</div> : tasks.length ? tasks.map((task) => <article className={`task-row ${task.status}`} key={task._id}><button className="check" onClick={() => toggle(task)}>{task.status === 'completed' && <Check size={14}/>}</button><div className="task-content"><h3>{task.title}</h3>{task.description && <p>{task.description}</p>}<div className="task-meta"><span className={`priority ${task.priority}`}>{task.priority}</span>{task.dueDate && <span>Due {new Date(task.dueDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric' })}</span>}</div></div><div className="row-actions"><Link to={`/tasks/${task._id}/edit`} title="Edit"><Edit3 size={16}/></Link><button onClick={() => remove(task._id)} title="Delete"><Trash2 size={16}/></button></div></article>) : <div className="empty">No tasks match these filters.</div>}</div></section> }
